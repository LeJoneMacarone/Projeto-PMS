<nav class="navbar navbar-expand-lg navbar-custom">
    <div class="container-fluid">
        <a href="#" class="navbar-brand">
            <img src="/images/logo.png" width="118" height="100" class="d-inline-block align-top">
        </a>
        <div class="collapse navbar-collapse">
            <ul class="navbar-nav mr-auto">
                <% if(user.localeCompare("creator")==0 || user.localeCompare("admin")==0) { %>
                    <li class="nav-item">
                        <a href="" class="nav-link">Campaigns</a>
                    </li>
                <% } %>

                <% if(user.localeCompare("admin")==0) { %>
                    <li class="nav-item">
                        <a href="" class="nav-link">Campaigns Creators</a>
                    </li>
                    <li class="nav-item">
                        <a href="" class="nav-link">Reports</a>
                    </li>

                <% } %>

                <% if(user.localeCompare("root_admin")==0) { %>
                    <li class="nav-item">
                        <a href="" class="nav-link">Add Admins</a>
                    </li>
                <% } %>
            </ul>


            <% if(user !==null && user.trim() !=="" ) { %>
                <div class="dropdown">
                    <button class="btn dropdown-toggle" type="button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        <i class="bi bi-person-circle profile-icon"></i>
                    </button> <!-- aqui tava o v -->
                    <div class="dropdown-menu dropdown-menu-right" aria-labelledby="dropdownMenuButton">
                        <!-- <img src="" alt="profile"> -->
                        <a class="dropdown-item" href="#">Profile</a>
                        <a class="dropdown-item" href="#">Logout</a>
                    </div>
                </div>
            <% } %>
        </div>
    </div>
</nav>